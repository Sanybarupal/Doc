# Git Tag

Tags mainly specific points (jaise releases v1.0.0, v2.0) ko mark karne ke liye use hote hain. Ye ek bookmark ki tarah hote hain.

## Example
Naya tag create karna:
```bash
git tag v1.0.0
```

Tags ko server par push karna:
```bash
git push origin v1.0.0
```