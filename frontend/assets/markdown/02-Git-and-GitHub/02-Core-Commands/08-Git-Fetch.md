# Git Fetch

`git fetch` aur `git pull` mein chota sa difference hai. Fetch sirf remote server se naye changes ki detail laata hai (download karta hai), lekin unhe aapke code mein merge nahi karta. Aap un changes ko pehle dekh sakte hain.

## Example
```bash
git fetch origin
```