# Git Push

`git push` aapke local commits (jo aapne PC par save kiye) ko remote server (jaise GitHub) par bhej (upload) deta hai.

## Example
```bash
git push origin main
```

Yahan `origin` aapke remote server ka naam hai aur `main` aapki branch ka naam.