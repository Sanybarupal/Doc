# Git Diff

`git diff` se aapko pata chalta hai ki aapne file mein exactly kya lines add ya delete ki hain, unhe stage ya commit karne se pehle.

## Example
```bash
git diff
```

Sirf staged files (jinhe git add kar diya hai) ke changes dekhne ke liye:
```bash
git diff --staged
```