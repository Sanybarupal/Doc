# Git Remote

`git remote` command local aur remote (GitHub) repositories ko connect karne ke kaam aati hai.

Aap dekh sakte hain ki aapka project kis server se juda hai:
```bash
git remote -v
```

Naya remote link add karna:
```bash
git remote add origin https://github.com/user/repo.git
```