# JS Variables
Variables data store karne ke containers hote hain. JavaScript mein hum `var`, `let`, ya `const` ka use karte hain.

```javascript
let name = "Rahul";
const age = 25;
var isStudent = true;
```