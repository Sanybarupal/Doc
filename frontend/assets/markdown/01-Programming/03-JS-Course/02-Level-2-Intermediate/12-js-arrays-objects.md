# JS Arrays & Objects
Arrays multiple values store karte hain, jabki Objects key-value pairs store karte hain.

```javascript
let colors = ["Red", "Green", "Blue"]; // Array

let person = { // Object
  name: "Amit",
  age: 30
};
```