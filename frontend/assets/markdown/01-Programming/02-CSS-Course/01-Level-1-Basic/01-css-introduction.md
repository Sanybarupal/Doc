# CSS Introduction
CSS (Cascading Style Sheets) ka use HTML elements ko design (style) karne ke liye hota hai. Ye batata hai ki HTML elements screen par kaise dikhenge.

## Example:
```css
body {
  background-color: lightblue;
}
h1 {
  color: white;
  text-align: center;
}
```