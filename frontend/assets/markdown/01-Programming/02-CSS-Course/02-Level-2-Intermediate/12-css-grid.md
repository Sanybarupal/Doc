# CSS Grid
Grid layout 2D layout system hai (rows aur columns dono). Ye complex web layouts banane ke liye use hota hai.

```css
.grid-container {
  display: grid;
  grid-template-columns: auto auto auto;
  gap: 10px;
}
```