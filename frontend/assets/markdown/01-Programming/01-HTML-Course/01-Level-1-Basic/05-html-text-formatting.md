# HTML Text Formatting
Text ko bold, italic, underline aadi karne ke liye formatting tags use hote hain.

- `<b>` aur `<strong>` Bold ke liye
- `<i>` aur `<em>` Italic ke liye
- `<u>` Underline ke liye