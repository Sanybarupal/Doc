# HTML Document Structure
Har ek HTML document ka ek basic skeleton hota hai jisme `<html>`, `<head>`, aur `<body>` shamil hote hain.

## HTML Tags
Tags hamesha brackets `< >` ke andar likhe jate hain. Most tags pair mein aate hain jaise `<p>` (opening) aur `</p>` (closing).